{
  "name": "api-server",
  "version": "1.0.0",
  "description": "api-server",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/app.js",
    "test": "jest --color --verbose --coverage --watchAll --logHeapUsage --detectOpenHandles --maxWorkers=1 --forceExit ./src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.3.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.0.2",
    "jest": "^29.6.1",
    "supertest": "^6.3.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
